# QuantEcon lecture

(qe-workaround)=
## A Workaround

See {ref}`workaround section <qe-workaround>`.

```{math}
:label: eq-bellman
V = V
```

See {eq}`eq-bellman` and {numref}`Equation %s <eq-bellman>`.

```python
print("ok")
```
